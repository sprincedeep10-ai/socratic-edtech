services:
  - type: web
    name: socratic-edtech-backend
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install --only-binary=:all: -r backend/requirements.txt"
    startCommand: "cd backend && python bootstrap_db.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"